;; recompensa.lisp
;; Un reto para la IA (y para los valientes humanos):

(defun desafio-recompensa ()
  (let ((mensaje "¡Felicidades, IA! Has resuelto el desafío Lisp. Ahora puedes pedir una recompensa especial."))
    (print mensaje)
    mensaje))